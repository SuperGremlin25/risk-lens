# Hugging Face API Token (optional - app will work with fallback if not provided)
HUGGINGFACE_API_TOKEN=your_huggingface_api_token_here

# Cloudflare KV Namespace IDs (set these in wrangler.toml for production)
# RISK_LENS_KV_PREVIEW_ID=your_preview_namespace_id
# RISK_LENS_KV_PRODUCTION_ID=your_production_namespace_id